plugins {
    id 'eu.xenit.docker'
}
repositories {
    jcenter()
}

dockerFile {
    dockerFile = file('Dockerfile')
    dockerBuild {
        repository = 'baseimage'
    }
}

buildDockerImage {
    doLast {
        project(':dependent').dockerAlfresco.baseImage = getImageId()
    }
}
