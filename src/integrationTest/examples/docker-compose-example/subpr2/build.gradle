plugins {
    id "eu.xenit.docker"
}

createDockerFile {
    from('alpine:edge')
}

dockerFile {
    dockerFile = createDockerFile.getDestFile()
    dockerBuild {
        repository = 'docker-compose-example-subpr1'
    }
}
